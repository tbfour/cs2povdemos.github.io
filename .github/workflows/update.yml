name: Refresh catalogue

on:
  schedule:
    - cron: '0 8 * * *'   # daily at 08:00 UTC (03:00 in Chicago during DST, 02:00 in winter)
  workflow_dispatch:

concurrency:
  group: refresh-catalogue
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      YT_API_KEY: ${{ secrets.YT_API_KEY }}   # <-- available to scraper step

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # Choose a single dep manager; here we use requirements.txt
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        run: python src/fetch_videos.py

      # Reads the freshly written videos.json, so it must follow the scraper.
      # Images already on disk are skipped, so this only fetches players new to
      # the catalogue. Non-fatal: a Liquipedia outage should not hold back the
      # videos.json refresh, and missing images fall back to silhouettes.
      - name: Fetch player & map images
        continue-on-error: true
        run: python src/fetch_images.py

      - name: Commit & push updated catalogue
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          file_pattern: docs/data/videos.json docs/data/images.json docs/img
          branch: main
          commit_message: "bot: refresh catalogue ${{ github.run_number }} [skip ci]"
          commit_user_name: tbfour-cs2povdemo-bot
          commit_user_email: 107200677+tbfour@users.noreply.github.com
