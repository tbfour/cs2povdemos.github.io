name: Refresh catalogue
on:
  schedule:       # hourly
    - cron:  '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    environment: YT_API_KEY              # 👈 must match the environment name exactly
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pipenv
      - run: pipenv install --deploy --system
      - name: Run scraper
        run: python src/fetch_videos.py
        env:
          YT_API_KEY: ${{ secrets.YT_API_KEY }}

        # ── Commit & push catalogue if it changed ────────────────────────────────────
      - name: Commit & push updated videos.json
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          file_pattern: data/videos.json
          branch: main                    # or refs/heads/main both work
          commit_message: "bot: refresh catalogue ${{ github.run_number }} [skip ci]"
          push_options: '--force'
          commit_user_name: 'tbfour'
          commit_user_email: 'taggertbaker@gmail.com'
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}   # fine-grained PAT (contents:write)
# ─────────────────────────────────────────────────────────────────────────────
